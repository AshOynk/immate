# MongoDB Atlas connection string (backend - Railway)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/compliance?retryWrites=true&w=majority

# JWT secret for login sessions (change in production)
JWT_SECRET=your-secret-at-least-32-chars

# Seed admin login: log in as ash@oynk.co.uk with this password (creates admin if needed). Optional locally (use immate-dev).
# SEED_LOGIN_PASSWORD=your-chosen-password

# Claude API key for compliance video quality + live-recording check (backend)
ANTHROPIC_API_KEY=sk-ant-...

# Eufy: show management what's done and trigger checks (optional). CUFY_* also supported.
# EUFY_WEBHOOK_URL=https://your-eufy-webhook.example.com/events
# EUFY_API_KEY=optional

# Voucher tiers JSON (optional). Default: 5/12/25/50 stars -> £5/£10/£25/£50
# VOUCHER_TIERS=[{"stars":5,"value":5,"label":"£5 voucher"},{"stars":12,"value":10,"label":"£10 voucher"}]

# Resident dashboard: weekly star target for bonus (default 10). Hit target = double XP + bonus voucher.
# WEEKLY_STAR_TARGET=10

# Optional: backend API URL (frontend - Vercel). Leave empty for same-origin (proxy).
# VITE_API_URL=https://your-app.railway.app
